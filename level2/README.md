# Level 2

Standalone Maven project for the Entelect Hackathon Level 2 strategy.

## Requirements

- Java 17 or newer
- Maven 3.8 or newer

## Build and run

From this directory:

```powershell
mvn clean package
java -cp target/classes level2.Main
```

The program writes `level2_solution.json` to the current directory.
