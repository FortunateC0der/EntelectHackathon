# Level 4

Standalone Maven project for the Entelect Hackathon Level 4 strategy.

## Requirements

- Java 17 or newer
- Maven 3.8 or newer

## Build and run

From this directory:

```powershell
mvn clean package
java -cp target/classes level4.Main
```

The program writes `level4_solution.json` to the current directory.
